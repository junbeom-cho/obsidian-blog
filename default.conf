server {
    listen 80;
    # 변경됨: 마운트된 부모 폴더 안의 public을 웹 루트로 지정
    root /usr/share/nginx/html/public;
    index index.html;
    error_page 404 /404.html;

    location / {
        try_files $uri $uri.html $uri/ /404.html;
    }
}
